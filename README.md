# node-chat-app

 Run **npm install** in the terminal to install the required dependencies.
 
 Run the chat app using the command **npm run start**.
 
 The file _index.js_ contains the server side code and _chat.js_ contains client side code.
